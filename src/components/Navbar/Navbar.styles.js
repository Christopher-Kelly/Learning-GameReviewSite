import { colours } from '../../styles/colours';

export const navbarStyles = {
    brand: {
        color: colours.text,
        fontWeight: 700,
    },
    navLink: {
        color: colours.text,
    },
};
